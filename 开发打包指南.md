# 实时语音通话插件 — 开发与打包指南

本指南帮助你从零搭建「实时语音通话」插件：在 Roche APK 中集成 Vosk 离线中文语音识别、悬浮语音球、多引擎 STT/TTS，并打包成可安装的 APK。

---

## 一、插件能做什么

- 仿手机电话通话界面，选角色 → 实时语音/文字对话
- **Vosk 离线识别**：完全免费、断网可用、流式实时出字（需打包进 APK）
- **Transformers.js 浏览器内 Whisper**：纯前端 WASM/WebGPU 推理，完全免费、零配置、不上传音频
- **悬浮语音球**：任意页面点一下录音、再点一下把语音转文字注入聊天输入框
  - **后台保活**：插件面板关闭后悬浮球仍保留，可在任意页面（含主聊天页）继续录音注入
  - **智能注入**：优先注入当前聚焦的输入框，再扫描页面所有可见的 input/textarea/contenteditable（支持通话界面、主聊天页、任意可输入区）
  - **自动回车**：注入文字后模拟 Enter 键尝试发送（可设置开关）
- **情绪识别**：提取说话时的音量/音高/音色，映射成情绪标签注入提示词，char 能感知你的语气
- 多 STT 引擎可切换：Vosk / Transformers.js / Web Speech API / Groq Whisper / 百度 / 小米 MiMo / 腾讯 / Wit.ai / 自托管 Whisper
- 多 TTS 引擎：Roche 内置 / MiniMax / ElevenLabs
- **通话历史**：挂断时弹窗选择保留/丢弃，保留的通话记录存进插件本地历史列表，可随时查看/删除/清空
- 通话记录可同步到角色单聊会话，让 char 记得通话内容（与通话历史互相独立）
- **提示词透明化**：设置页可一键查看实际发给模型的完整 system prompt（含强制追加的格式规范）
- 每角色独立配置（背景 / voiceId / 记忆挂载 / 世界书 / 绑定 user 人设）

---

## 二、前置条件

| 项 | 要求 |
|----|------|
| Roche 源码项目 | 已用 Capacitor 框架，且你之前成功打包过 APK |
| Node.js | 14+ |
| Android Studio | 已安装 |
| JDK | 11+ |
| Capacitor | 5.0+（看 `package.json` 里 `@capacitor/core` 版本） |
| 手机 | Android 7.0（API 24）及以上 |

---

## 三、获取插件代码

插件源码托管在 GitHub（luyi 仓库）：

```
https://github.com/luyi90720-sys/roche-realtime-voice-call
```

仓库里只有两个文件是插件本体：

| 文件 | 作用 |
|------|------|
| `plugin.js` | 插件全部逻辑（通话界面、Vosk 调用、悬浮球、设置页等） |
| `manifest.json` | 插件清单（权限声明、入口地址） |

把这两个文件放到你 Roche 项目的插件目录（通常是 `plugins/realtime-voice-call/`），或按你 Roche 的插件加载方式放置。

---

## 四、安装 Vosk 离线识别插件

Vosk 识别跑在 Android 原生层，需要把 `capacitor-offline-speech-recognition` 装进 Roche 项目。

### 4.1 安装 npm 包

在 Roche 项目根目录（`package.json` 所在目录）执行：

```bash
npm install capacitor-offline-speech-recognition
npx cap sync
```

验证：`node_modules/capacitor-offline-speech-recognition` 应该存在。

> 如果失败：检查网络能否访问 npmjs.com；检查 Capacitor 是否 5.0+，4.x 需降级插件或升级 Capacitor。

### 4.2 注册插件到 MainActivity

找到文件（包名看 `android/app/build.gradle` 里 `applicationId`）：

```
android/app/src/main/java/<你的包名>/MainActivity.java
```

修改为：

```java
package com.example.roche;  // 你自己的包名

import android.os.Bundle;
import com.getcapacitor.BridgeActivity;

// 新增这一行（插件 3.0.0+ 的正确包名）
import dev.ravi.offlinespeech.OfflineSpeechRecognitionPlugin;

public class MainActivity extends BridgeActivity {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        // 新增这一行（必须在 super.onCreate 之前）
        registerPlugin(OfflineSpeechRecognitionPlugin.class);
        super.onCreate(savedInstanceState);
    }
}
```

> 注意：旧包名 `com.gaudravi09.capacitorofflinespeechrecognition.CapacitorOfflineSpeechRecognitionPlugin` 是 2.x 版本的，3.0.0+ 必须用 `dev.ravi.offlinespeech.OfflineSpeechRecognitionPlugin`，否则报 `cannot find symbol`。

### 4.3 配置 Android 权限

检查 `android/app/src/main/AndroidManifest.xml`，确保有：

```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.INTERNET" />
```

- `RECORD_AUDIO`：录音（需用户授权）
- `MODIFY_AUDIO_SETTINGS`：音频设置（normal 权限，安装时自动授予，必须加，否则 Capacitor 权限请求会失败）
- `INTERNET`：首次下载中文模型用，下载完即可离线

### 4.4 检查 build.gradle

打开 `android/app/build.gradle`，确保：

```groovy
android {
    compileSdkVersion 34
    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 34
    }
}
```

`minSdkVersion` 低于 24 会编译失败。

---

## 五、优化 Vosk 中文识别准确率（关键）

> 默认配置下 Vosk 中文识别准确率偏低，原因是 Vosk 插件用了**原始麦克风**采集（带环境噪声），没有启用 Android 为语音识别调优的降噪处理。本节用一个 patch 修复，能明显改善嘈杂环境下的识别效果。

### 5.1 问题根因

Vosk 插件源码 `OfflineSpeechRecognitionPlugin.java` 的 `startAudioRecording()` 方法里：

```java
// 原始代码（问题所在）
audioRecord = new AudioRecord(
    MediaRecorder.AudioSource.MIC,   // 原始麦克风，采集所有噪声
    sampleRate, channelConfig, audioFormat, bufferSize
);
```

`AudioSource.MIC` 是原始麦克风输入，不做任何处理。应改用 `AudioSource.VOICE_RECOGNITION`，它会启用 Android 为语音识别调优的**自动降噪 + 回声消除 + 自动增益（AGC）**。

### 5.2 安装 patch-package

在 Roche 项目根目录：

```bash
npm install --save-dev patch-package
```

然后在 `package.json` 的 `scripts` 里加 postinstall 钩子：

```json
{
  "scripts": {
    "postinstall": "patch-package"
  }
}
```

### 5.3 修改插件源码并生成补丁

打开文件：

```
node_modules/capacitor-offline-speech-recognition/android/src/main/java/dev/ravi/offlinespeech/OfflineSpeechRecognitionPlugin.java
```

找到 `startAudioRecording()` 方法里的这一行（约 319 行）：

```java
audioRecord = new AudioRecord(MediaRecorder.AudioSource.MIC, sampleRate, channelConfig, audioFormat, bufferSize);
```

改成：

```java
audioRecord = new AudioRecord(MediaRecorder.AudioSource.VOICE_RECOGNITION, sampleRate, channelConfig, audioFormat, bufferSize);
```

保存后，在 Roche 项目根目录执行：

```bash
npx patch-package capacitor-offline-speech-recognition
```

这会在项目根目录生成 `patches/capacitor-offline-speech-recognition+x.x.x.patch` 文件。以后每次 `npm install` 都会自动应用这个补丁，不会丢失。

> 验证补丁生成成功：`patches/` 目录下应有 `.patch` 文件，内容里能看到 `MIC` → `VOICE_RECOGNITION` 的 diff。

### 5.4 关于进一步提升准确率

Vosk 默认下载的是小模型 `vosk-model-small-cn-0.22`（约 42MB，准确率约 85%）。若修复音频源后仍不够准，可考虑换大模型 `vosk-model-cn-0.22`（约 1.3GB，准确率约 92%），方法：在同一个 `ModelDownloadManager.java` 里把 `model-zh` 的 URL 从 `vosk-model-small-cn-0.22.zip` 改为 `vosk-model-cn-0.22.zip`，同样用 patch-package 固化。代价是首次下载 1.3GB、占用存储 1.3GB。

---

## 六、打包 APK

### 6.1 同步并打开 Android Studio

```bash
# 在 Roche 项目根目录
npx cap sync android

# 打开 Android 项目
npx cap open android
```

### 6.2 构建

在 Android Studio 里：`Build → Build Bundle(s)/APK(s) → Build APK(s)`

或用命令行：

```bash
cd android
./gradlew assembleDebug     # debug 版
# 或
./gradlew assembleRelease   # release 版（需配置签名）
```

### 6.3 APK 输出位置

```
android/app/build/outputs/apk/debug/app-debug.apk
android/app/build/outputs/apk/release/app-release.apk
```

### 6.4 不需要做的事

- 不用手动下载 Vosk 模型文件（用户首次使用时自动从 alphacephei.com 下载，约 42MB，下载后永久缓存）
- 不用往 assets 目录放东西
- 不用配置任何 API Key（Vosk 完全离线免费）

---

## 七、安装与验证

把 APK 装到手机后，验证：

1. 打开 Roche，进入实时语音通话插件，能进入设置页
2. 设置 → STT 引擎 → 下拉里有「Vosk 离线识别」选项
3. 选一个角色点通话，进入通话界面
4. 首次会提示「下载中文模型中... XX%」（约 42MB，需联网）
5. 模型下载完，对手机说话，实时出字
6. 停顿一会儿，识别文字自动发给 char
7. 断网再试，应仍能识别（模型已缓存）

### 悬浮语音球验证

1. 设置页顶部 → 找到「悬浮语音球」开关 → 开启（即时生效）
2. 屏幕右下角出现红色悬浮球
3. 拖动可移动位置；点一下开始录音（球变红脉动）
4. 对手机说话，悬浮球上方实时显示识别文字
5. 再点一下停止，识别文字智能注入当前聚焦的输入框（通话输入框/主聊天输入框 `.chat-input-textarea`/任意可见 input/textarea/contenteditable）
6. 若开启「注入后自动回车发送」，注入后自动模拟 Enter 键尝试发送；界面若用按钮发送则不生效，关掉此项改为只注入
7. **后台保活**：关掉插件面板，悬浮球仍在屏幕上，可继续录音注入
8. **彻底关闭**：设置页「彻底关闭悬浮球（清理后台）」按钮 → 确认后停录音、移除球 DOM、清全局态、关设置

### 通话历史验证

1. 选角色通话，说几句话后挂断
2. 挂断时弹窗「保留通话记录？」→ 选「保留」
3. 返回列表页，点「通话历史」入口 → 能看到刚才的通话记录
4. 点记录卡片 → 弹窗展示通话消息气泡（user 红 / char 白 / sys 灰）
5. 单条记录右上角「X」可删除；顶栏「清空」可清空全部

### 提示词预览验证

1. 设置页 → 找到「自定义通话提示词」区 → 点「查看实际注入的完整提示词」按钮
2. 弹窗展示实际发给模型的 system prompt（占位符已用第一个角色替换演示）
3. 末尾可见强制追加的【通话语言】【中文翻译】两段（格式规范，不可省略）
4. 弹窗下方有红色小字提醒：默认提示词粗糙，建议自定义

---

## 八、插件配置说明

在插件设置页可配置：

| 配置 | 说明 |
|------|------|
| 通话语言 | char 回复用的语言（如 zh-CN、en-GB） |
| 识别语言 | STT 识别用的语言（可与通话语言不同，Vosk 按此选模型） |
| STT 引擎 | Vosk / Transformers.js / Web Speech / Groq / 百度 / 小米 / 腾讯 / Wit.ai / 自托管 Whisper |
| 通话模式 | auto（停顿自动发送）/ ptt（按住说话松开发送） |
| 通话记录同步 | 开启后挂断时把通话消息注入角色单聊会话 |
| 通话历史 | 挂断时弹窗选择保留/丢弃；保留的存进插件本地历史列表（列表页「通话历史」入口查看） |
| 悬浮语音球 | 开启后任意页面可用悬浮球录音转文字（设置页顶部，即时生效）；插件面板关闭后仍保留（后台保活）；「彻底关闭悬浮球」按钮可清理后台 |
| 注入后自动回车 | 悬浮球注入文字后模拟 Enter 键发送；界面用按钮发送时无效，关掉改为只注入 |
| 情绪识别 | 开启后用 MediaRecorder 路径录音时提取音量/音高/音色映射成情绪标签注入提示词（Vosk 路径无效，音频在原生层） |
| 提示词预览 | 点「查看实际注入的完整提示词」按钮，展示 system prompt + 强制追加的【通话语言】【中文翻译】 |
| 自定义提示词 | 留空用内置；支持 `{{char}} {{user}} {{persona}} {{user_persona}} {{memory}}` 占位符 |
| 通话 user 人设 | 每个 char 可绑定不同 user 人设（char 配置页选择），绑定后 `{{user}} {{user_persona}}` 用该 user 的信息 |
| 音频降噪 | VAD 触发阈值（越高越不易被噪声触发） |
| TTS 引擎 | Roche / MiniMax / ElevenLabs |

各 STT 引擎的 Key 在对应设置区填写。Vosk 与 Transformers.js 无需任何 Key。

### 关于「格式规范」

插件发给模型的 system prompt 由两部分组成：

1. **提示词主体**：内置默认提示词，或用户在「自定义通话提示词」里填写的内容（占位符已替换）
2. **强制追加的格式规范**（即使用户自定义提示词也不会省略）：
   - 【通话语言】：要求 AI 用指定语言回复
   - 【中文翻译】：外语通话且开启翻译时，要求 AI 末尾附加中文译文行

格式规范是插件正常解析 AI 回复（语言切换、翻译分离、TTS 播报）所必需的，因此不可省略。点「查看实际注入的完整提示词」按钮可看到这两部分的最终拼装结果。

---

## 九、常见问题

### Q1：`registerPlugin` 报错找不到方法
Capacitor 版本太低。升级到 5.0+，或用旧版语法 `this.registerPlugin(...)`。

### Q2：编译报错 `cannot find symbol OfflineSpeechRecognitionPlugin`
插件没装成功或 `npx cap sync` 没跑。重装：
```bash
npm uninstall capacitor-offline-speech-recognition
npm install capacitor-offline-speech-recognition
npx cap sync android
```

### Q3：插件装好但 WebView 里调不到
检查 `plugin.js` 里 `detectVoskAvailable()`，它检测 `Capacitor.Plugins.CapacitorOfflineSpeechRecognition` 或 `OfflineSpeechRecognition`。插件名不同需调整。

### Q4：麦克风权限被拒绝
即使系统设置已授权，仍提示被拒。原因是缺 `MODIFY_AUDIO_SETTINGS` 权限。在主 `AndroidManifest.xml` 加：
```xml
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
```
加完重新打包。详见 `plugin.js` 里 `ensureMicPermissionForVosk()` 的注释。

### Q5：提示「zh 语言模型未下载」
Vosk 用的是**识别语言**对应的模型，不是通话语言。在设置里把「识别语言」设为 `zh-CN`，然后到设置 → STT 引擎 → Vosk 设置 → 点「下载模型」。模型约 42MB，首次需联网。

### Q6：识别错用了通话语言（如本该中文却下载英文模型）
早期版本有此 bug，已修复：所有 Vosk 调用（启动识别、下载模型、模型检测）统一用识别语言 `state.settings.lang`，不再用通话语言 `callLang`。请拉取最新 `plugin.js`。

### Q7：识别准确率低
默认用原始麦克风采集。按本指南第五节用 patch-package 把 `AudioSource.MIC` 改为 `VOICE_RECOGNITION`，启用 Android 降噪。改完重新打包。

### Q8：Vosk 中文结果带空格
Vosk 中文小模型默认输出分词空格（如「我 是 中国人」）。`plugin.js` 里 `cleanVoskText()` 已自动去除中文空格、合并重复标点。若仍异常，确认用的是最新 `plugin.js`。

### Q9：快速切歌/快速操作报 `play() request was interrupted`
这是浏览器音频竞态，`plugin.js` 已静默处理，无需理会。

### Q10：下载模型失败
检查手机能否访问 `https://alphacephei.com`；检查 app 存储权限；换网络重试。

### Q11：悬浮球关掉插件面板后消失了
旧版本有此问题，已修复。最新版采用独立样式表（`#rvc-float-styles`，unmount 不移除）+ 全局态（`window._rvcFloat`）保活机制。关插件面板后悬浮球仍保留，可继续录音注入。若要真正关闭，在设置页点「彻底关闭悬浮球（清理后台）」按钮。请拉取最新 `plugin.js`。

### Q12：情绪识别对 Vosk 无效
这是技术限制，非 bug。Vosk 流式识别的音频在 Android 原生层处理，JS 拿不到 PCM 音频数据，无法提取声学特征。情绪识别仅在 MediaRecorder 路径（webkit/Groq/MiMo/百度/腾讯/Wit/Transformers.js/自托管 Whisper）生效。Vosk 模式下开关不注入标签，不影响原识别功能。

### Q13：悬浮球注入后没自动发送
「注入后自动回车发送」通过模拟 Enter 键事件触发发送。若通话/聊天界面用按钮点击发送而非监听 Enter 键，则无法触发。关掉此项改为只注入文字，由用户手动点发送按钮。实测若界面支持回车发送则直接生效。

---

## 十、plugin.js 里 Vosk 相关代码位置

调试时可定位：

| 功能 | 函数 | 作用 |
|------|------|------|
| 引擎检测 | `detectVoskAvailable()` | 检测 APK 里有没有 Vosk 插件 |
| 启动识别 | `startVoskListening()` | 启动流式识别 + 监听结果 |
| 停止识别 | `stopVoskListening()` | 停止并清理监听器 |
| 文本清洗 | `cleanVoskText(text, lang)` | 中文去空格、英文压缩空格、标点规范化 |
| 路由分发 | `startListening()` 里 `provider === 'vosk'` 分支 | 统一入口分发到 Vosk |
| 悬浮球录音 | `startFloatRecording()` / `stopFloatRecording()` | 悬浮球独立录音→注入输入框 |
| 悬浮球保活 | `window._rvcFloat` 全局态 + `#rvc-float-styles` 独立样式 | unmount 不清，面板关闭后悬浮球保留 |
| 彻底关闭 | `forceShutdownFloatBall()` | 停录音+移除 DOM+清全局态+关设置 |
| 智能注入 | `findInjectTarget()` / `injectToChatInput(text)` | 优先当前聚焦元素，再扫所有可见输入框 |
| 模拟回车 | `simulateEnterKey(el)` | 注入后模拟 Enter 键事件尝试发送 |
| 情绪识别 | `createEmotionTracker()` / `updateEmotionFeatures()` / `computeEmotionTags()` | 声学特征→情绪标签注入提示词 |

---

## 十一、隐私与安全

本插件尊重用户隐私，代码全部开源可审计。以下是隐私安全声明：

### 数据存储

- 所有用户数据（设置、API Key、通话历史）仅通过 `roche.storage` 存在**本地设备**，不上云、不联网。
- 通话历史最多保留 100 条，存在本地 `rvc_call_logs` 键下，可随时清空。
- API Key 存在本地 `rvc_settings` 键下，输入框用 `type="password"` 隐藏明文。

### 网络请求

- 插件发起的所有网络请求（共 9 处）均指向**用户主动配置 Key 的正规 STT/TTS 服务商官方 API**（Groq、百度、小米、腾讯、Wit.ai、MiniMax、ElevenLabs）。
- 每个请求前都有 Key 存在性校验：用户不填 Key 的服务商不会被调用。
- **无任何统计、埋点、遥测、上报代码**。
- **无任何 phone home 行为**（不向作者服务器发请求）。

### 离线识别

- **Vosk 识别完全本地**：识别在设备原生层完成，不发任何网络请求；模型首次下载后永久缓存离线用。
- **Transformers.js 浏览器内 Whisper**：模型在浏览器本地 WASM/WebGPU 跑，用户音频不上传。
- 这两个引擎的语音数据**完全不离开设备**。

### 情绪识别（本地处理）

- **纯前端声学特征提取**：用 Web Audio API 的 `AnalyserNode` 在本地提取音量(RMS)/过零率(音高)/频谱质心(音色)，规则映射成情绪标签。
- **不上传音频**：所有计算在浏览器本地完成，原始音频和特征数据都不离开设备。
- **仅 MediaRecorder 路径生效**：Vosk 流式识别的音频在 Android 原生层，JS 拿不到 PCM，此开关对 Vosk 无效（不注入标签，不影响原功能）。
- 规则较粗糙（纯前端无法精确区分"激动"和"愤怒"），仅作参考增强。

### 代码安全

- **无 `eval()`、`new Function()`、`document.write()`** 等危险操作。
- **无 `postMessage`、`sendBeacon`、`WebSocket`** 等隐蔽外发通道。
- **不读取浏览器 Cookie / UserAgent / localStorage**（只读写插件自己的 `roche.storage`）。
- console 日志仅 4 处，只打印系统状态文案，不打印 API Key、用户语音文本、Cookie 等敏感信息。

### 需用户知悉的两点（非缺陷）

1. **百度 SecretKey**：浏览器端换取 access_token 时，SecretKey 会出现在网络请求 URL 里（百度 OAuth 官方流程限制），UI 已提示「仅自用」。介意者请改用其他引擎。
2. **Groq Whisper prompt hint**：调用 Groq 时会附带「角色名 + 用户名」作为识别提示（帮助 Whisper 识别专有名词），发给用户自己配置的 Groq 端点。

### 第三方代码

- `plugin.js` 是**纯自研代码**，无任何拷贝/引用第三方 JS 库。
- 唯一外部动态加载：`@huggingface/transformers@3.0.0`（从 jsDelivr CDN 加载，HuggingFace 官方库，固定版本号，本地推理不上传数据）。
- Vosk 识别引擎（`capacitor-offline-speech-recognition`）是第三方 Capacitor 插件，原生层代码，仅作为依赖打包进 APK，plugin.js 不修改其行为。

---

## 十二、仓库与许可

- 插件源码仓库：https://github.com/luyi90720-sys/roche-realtime-voice-call
- Vosk Capacitor 插件：https://github.com/gaudravi09/capacitor-offline-speech-recognition
- Vosk 引擎与模型：https://alphacephei.com/vosk

---

## 十三、快速流程总结

1. `npm install capacitor-offline-speech-recognition` + `npx cap sync`
2. `MainActivity.java` 注册插件（2 行改动）
3. `AndroidManifest.xml` 加 `RECORD_AUDIO` + `MODIFY_AUDIO_SETTINGS` 权限
4. （推荐）按第五节用 patch-package 把音频源改为 `VOICE_RECOGNITION`
5. `npx cap sync android` → `npx cap open android` → Build APK
6. 装 APK，进插件设置页开启 Vosk 引擎、下载中文模型
7. 想用悬浮球：设置页开启「悬浮语音球」开关（关插件面板后仍保活，想真正关点「彻底关闭悬浮球」按钮）
8. 想自动发送：设置页开「注入后自动回车发送」（界面用回车发送才生效）
9. 想让 char 感知语气：设置页开「情绪识别」（仅 MediaRecorder 路径生效，Vosk 无效）
